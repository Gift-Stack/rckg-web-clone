import { render, screen } from "@testing-library/react";
import SpotHistoryGroup from "./SpotHistoryGroup";

describe("SpotHistoryGroup", () => {
	it.todo("SpotHistoryGroup");
	// it("should render correctly", () => {
	// 	render(<SpotHistoryGroup isAuthenticated={false} />);
	// 	const notAuthEl = screen.getByTestId("not-logged-in");
	// 	expect(notAuthEl).toBeInTheDocument();
	// });
	// it("should not render content when not authenticated", () => {
	// 	render(<SpotHistoryGroup isAuthenticated={false} />);
	// 	const loggedInEl = screen.queryByTestId("not-logged-in");
	// 	expect(loggedInEl).toBeInTheDocument();
	// });
	// it("should render content when authenticated", () => {
	// 	render(<SpotHistoryGroup isAuthenticated={true} />);
	// 	const loggedInEl = screen.getByTestId("logged-in");
	// 	expect(loggedInEl).toBeInTheDocument();
	// });
});
