import { fireEvent, render, screen } from "@testing-library/react";
import { act } from "react-dom/test-utils";
import Market from ".";

describe("Limit", () => {
	it.todo("Limit");
	// it("renders properly", async () => {
	// 	const setValues = jest.fn();
	// 	const setTradeRate = jest.fn();
	// 	render(
	// 		<Market
	// 			btnState={"BUY"}
	// 			setValues={setValues}
	// 			setTradeRate={setTradeRate}
	// 		/>
	// 	);
	// 	const trade_limit = screen.getByTestId("rg-trade-market");
	// 	act(() => {
	// 		fireEvent.submit(trade_limit);
	// 	});
	// 	expect(trade_limit).toBeInTheDocument();
	// });
});
