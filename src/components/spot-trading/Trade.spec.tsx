import { render, screen } from "@testing-library/react";
import Trade from "./Trade";

describe("Trade", () => {
	it("renders properly", async () => {
		// render(
		// 	<Trade
		// 		tradeTab={{
		// 			id: 1,
		// 			name: ["Spot"],
		// 		}}
		// 	/>
		// );
		// const trade = screen.getByTestId("rg-trade");
		// const trade_limit = screen.getByTestId("rg-trade-limit");
		// const trade_witch = screen.getByTestId("trade-switch");
		// const spot_tab = screen.getByTestId("rg-spot-tabs");
		// const spot_tabs = screen.getAllByTestId("rg-spot-tab");
		// const trade_switches = screen.getByTestId("rg-trade-switches");
		// expect(trade).toBeInTheDocument();
		// expect(trade_limit).toBeInTheDocument();
		// expect(trade_witch).toBeInTheDocument();
		// expect(spot_tab).toBeInTheDocument();
		// expect(spot_tabs.length).toBe(3);
		// expect(trade_switches.children.length).toBe(1);
	});
});
