import { generateActions } from "../../utils";

export const USER_IP = generateActions("USER_IP");
export const USER_DEVICE = generateActions("USER_DEVICE");
export const USER_GEO_DATA = generateActions("USER_GEO_DATA_DEVICE");
